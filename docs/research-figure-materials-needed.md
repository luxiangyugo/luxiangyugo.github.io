# Remaining cross-focus figure material

The spatial half now uses actual archived MFL104 prototype screenshots. Florence-2 uses actual inspection images, recorded prompt excerpts, and geometry traced from documented localization outputs. No generated image is used as experimental evidence.

The requested cross-focus image strip and response curve cannot yet be completed from the material accessible in this task. The detailed plate, accessible through the “Prototype outputs” button, explicitly marks this missing evidence and does not draw a synthetic curve. The current page illustration is explicitly conceptual and does not stand in for these missing measurements.

The newest page image includes an explicitly labeled illustrative D/N trajectory comparison over 25 focus layers, as requested by the owner. This conceptual graph does not close the measured-data gap below and must not be relabeled as an experimental result.

The smallest useful addition is one matched MFL103 sample folder containing:

1. `MFL103_05_track_overlay.png`, identifying the fixed spatial position and track number.
2. `MFL103_11_track_003_focus_strip.png` (or the corresponding chosen track), plus `MFL103_06_track_focus_curves.png` from the same sequence. Prefer the per-layer response CSV/NPZ and original focus crops so the curve can be replotted without digitizing a screenshot.
3. A matched normal-region trajectory using the same response definition, if available. Keep a global normal-layer envelope separate from a local trajectory; their aggregation statistics are not interchangeable.

Include the sequence identifier, ordered focus indices, ROI coordinates, and the definition of the plotted response. A dataset folder or archive containing these outputs is sufficient. Previous research notes identify the result folder as `D:/code/MultiFocusLens/analysis/MFL103_focus_track_results`; that folder is not present on this host.

Do not smooth the data into a prescribed single peak, equate persistence with defects, or claim cross-batch/model gains before validation. The plotted conclusion should concern amplitude and shape relative to normal focus evolution.

For a fully measured lens grounding panel, also supply the Florence-2 candidate box for the shown lens. The current dashed lens box is explicitly a schematic candidate outline; the final circle follows a recorded output.
