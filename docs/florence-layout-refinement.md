# Florence-2 alignment and whitespace revision

Historical revision, superseded by [the companion-style correction](florence-companion-style.md). The 4:3 layout below was rejected because it elongated objects. The active asset now preserves natural proportions and removes outer margin with a literal crop.

Built-in image_gen edits of `assets/images/florence2-model-grounding.png`; the final output replaces that same project asset at the owner's request. Research content, terminology, and neighboring figure are preserved. Dimensions are 1448 × 1086 (4:3).

Final source: `exec-f8858dcc-9d9e-4c43-b3ad-3bbfd7e74e89.png`. Intermediate alignment source: `exec-d9de7831-f0fd-43be-9076-4a6749128561.png`.

## Alignment prompt

Edit ONLY the layout, alignment and whitespace of this approved Florence-2 research illustration. Preserve all current text, optical renders, model modules, colors, arrow meanings and the pale mint/sage translucent style. The user approves the content and visual style.

Make the composition more compact and less horizontally stretched. Use a balanced 4:3 landscape canvas, rather than a long horizontal strip floating in large blank top/bottom margins. Arrange the content to occupy almost the full canvas, with small even outer margins of about 3–4%.

Align the four major column groups precisely: the combined Images/Text prompts group, the Florence-2 model, Grounded regions, and the combined ROI refinement/AOI group should share the same TOP and BOTTOM baselines. In the last column, move ROI refinement up so its top edge aligns with the left/model panels. Place AOI neatly directly beneath it, centered within that same column, with its bottom edge aligned with the bottoms of the left/model panels. It must no longer dangle below all the other columns. Keep a consistent small vertical gap between ROI refinement and AOI. Keep the sensor-to-AOI and downstream connections clear and short.

Reduce horizontal gaps and rebalance column widths. Reflow labels neatly and make the optics/model contents comfortably larger vertically to use the available height; do not simply stretch or distort pixels. Eliminate the excessive empty band above and below the diagram. Keep arrows tidy, labels readable at webpage size, and all elements inside the frame. Keep the Florence-2 model dominant. Do not add or delete a semantic step, model layer, label, box, caption or decorative element. Keep the circle and rectangle as independent geometry examples. This remains the same conceptual scientific illustration, only better aligned and proportioned.

## Whitespace refinement prompt

One final layout correction only. Preserve this aligned 4:3 Florence-2 diagram, every word, all objects, modules, colors and arrow meanings. The top and bottom empty bands are still much too large. On this 1448 × 1086 composition, reposition and reflow all four column groups so their common top edge is near y=35 and common bottom edge near y=1045, instead of the current y=150 and y=865. Maintain their aligned top/bottom edges. Rebalance the contents vertically within the taller panels: enlarge the optical thumbnails and model blocks where space permits, space the internal rows comfortably, and keep the text clear. ROI refinement remains above AOI in the last column, with AOI bottom aligned to the other columns. The complete diagram should fill about 93–95% of canvas height and width, with only a narrow even border around it. Do not merely crop the canvas into a wide horizontal banner; retain the more compact 4:3 proportion. Do not distort or stretch images, add labels or introduce new content. Remove the large empty outer bands; keep the approved scientific glass-like style.
