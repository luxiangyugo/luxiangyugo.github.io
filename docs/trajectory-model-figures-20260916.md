# 25-layer trajectories and Florence-2 model illustrations

These project-bound PNGs are conceptual illustrations edited using the built-in image_gen tool. They are not experimental images or measured curves. The page keeps separate “Prototype outputs” links to archived real examples.

- Page asset: `assets/images/multi-focus-trajectories.png`; edit target: `assets/images/polar-fourier-optics.png`.
- Page asset: `assets/images/florence2-model-grounding.png`; edit target: `assets/images/florence2-multimodal-optics.png`.

Final generated sources: `exec-0326c656-4388-4ad4-8a12-c76e68a4df18.png` for the focus illustration and `exec-3458beae-b1dc-4390-85f2-12a04e75e7ce.png` for the model illustration. Both final sources are copied unmodified. The model image (1448 × 1086) places inputs and Florence-2 above grounded candidates and ROI refinement. The latest correction reserves teal/amber for target identity, uses neutral blue tones for modality tokens and replaces the sensor-to-AOI elbow with a straight arrow. Natural proportions, token interaction and lens overlays are preserved; see [two-tier layout and color record](florence-two-tier-layout.md). Token strips and interaction arrows abstract multimodal fusion, not a claim about token counts or an exact attention implementation.

The owner explicitly specified a 25-layer input. A stack notation may abbreviate visible planes; it does not imply that several distinct physical lenses are being stacked. The matched D/N regions and illustrative curves explain the proposed comparison of response strength and shape across focus. Normal responses may persist and vary across the same sequence. The drawn defect trajectory is one conceptual example, not a universal shape or a measured finding. No experimental performance or completed cross-focus model is claimed.

The simplified Florence-2 module follows [the original paper, Section 3 and Figure 2](https://arxiv.org/html/2311.06242v1): image encoder and multimodal encoder–decoder, with image features and text embeddings as inputs. The [official model card](https://huggingface.co/microsoft/Florence-2-large) documents prompt-based grounding and region outputs. This diagram does not claim a particular model size, exact implementation, fine-tuning setup, or direct precision circle prediction. Geometry remains a downstream step; the accompanying description retains the sensor outer-crop/inner-prompt workflow.

## Final focus edit prompt

Use case: scientific-educational. Edit the supplied concept illustration for a simple academic homepage. Preserve the premium lens cutaway, translucent glass, restrained sage-gray background, soft light and precise scientific visual style. Landscape 3:2; all text large and readable when reduced to 410px wide. Shift the research emphasis to 25-layer input and cross-focus trajectories. Remove the large Background and Residual panels. Composition: a compact elegant lens cutaway at upper left feeds a prominent perspective stack of inspection image planes in the lower-left/center, clearly labeled "25 focus layers". This is one optical component scanned through 25 focus indices, not 25 separate lenses. Depict a dense thin stack; an ellipsis may abbreviate the stack rather than pretending every layer is individually visible. The front grayscale annular lens image has an amber circular marker "D" at a small localized defect and a teal square marker "N" on normal ring structure. Maintain those fixed spatial coordinates on several visible planes. A small subdued support element beneath the stack reads "Polar–Fourier prior", with only one tiny unwrapped strip; this is a secondary structural prior, not the main output. On the right, ONE LARGE uncluttered focus-response plot visually connected from markers D and N. Use the same amber for D and teal for N; make direct legend labels "D · defect" and "N · normal". Both trajectories extend across the entire focus sequence. Normal response must vary smoothly and persist across focus, not be flat or zero. The illustrative defect trajectory should have asymmetric departures, a shoulder and a second local rise relative to the varying normal curve; NOT an ideal Gaussian or a single perfect spike. Do not suggest that persistence is sufficient to identify a defect. Plot horizontal axis "Focus layer", with only 1, 13 and 25 ticks, vertical axis "Response" without numerical values. Above or below the plot put the large readable words "Illustrative trajectories" so nobody mistakes this sketch for measured experiment. Make the corresponding image markers and curves unmistakably linked by color and a fine connector; no loose decorative lines. Keep the lens, stack and one graph balanced and visually large. No paragraphs, neural-network spaghetti, numerical performance, equations, extra graphs or irrelevant devices. Generated images and curves are conceptual only, not measured data. Small safe margins, no clipped elements.

## Model input-routing refinement

Correct the input grouping and arrow routing in this Florence-2 illustration, keeping the pale sage style, translucent model, optical renders, landscape 3:2, large Florence-2 heading and grounded output examples. The current figure misleadingly routes the SENSOR IMAGE into Prompt tokens and leaves Image tokens as a disconnected branch. Fix precisely:
1. Replace the two existing LEFT image-and-text cards with TWO MODALITY GROUPS. Top group labeled "Images" contains BOTH lens and sensor thumbnails, side by side, and no prompt text. A single arrow from this group enters "Image encoder".
2. Bottom-left group labeled "Text prompts" contains ONLY the phrases "circular camera lens" and "sensor window" in their respective teal/blue colors, no photograph. Its arrow enters "Prompt tokens" directly, bypassing the image encoder.
3. Inside Florence-2, make the ONLY image path: Image encoder -> Image tokens -> Encoder–decoder. Remove the existing direct bypass arrow from Image encoder into Encoder–decoder. Both image-token and prompt-token rows must visibly feed the same Encoder–decoder module with clear arrow endpoints.
4. Encoder–decoder -> Grounded regions -> ROI refinement remains the simple output path, outside the model. In ROI refinement, show circle and rectangle as independent example geometry icons; REMOVE the dashed arrows between them, which wrongly suggest a circle turns into a rectangle.
Keep outputs as candidate regions and the small sensor AOI as a subsequent region operation. No new claims or measured results. Simplify whitespace and use readable labels of about 42–46px at a 1536px source; do not add extra headings, model layers, paragraphs or duplicate images. Keep the same three major visual groups: inputs, model, outputs. This is a conceptual diagram based on an image encoder plus multimodal encoder–decoder, not an exact implementation schematic.

## Initial model edit prompt

Use case: scientific-educational. Edit the supplied lens/sensor concept illustration for a simple academic homepage. Keep the refined optical materials, pale sage background, soft shading and restrained teal/blue/amber colors, landscape 3:2. Reorganize it around an ACTUAL VISIBLE FLORENCE-2 MODEL MODULE, not a footer word. A clear left-to-right transition must read: image + language inputs -> Florence-2 model -> grounded detection regions -> secondary ROI refinement. Left: two compact inspection-image depictions of a lens and sensor plus two short visible text prompts, "circular camera lens" in teal and "sensor window" in blue. These are distinct image and text inputs, with separate arrows into the model. Center: one elegant translucent block labeled prominently "Florence-2", containing a simplified, scientifically grounded architecture: an "Image encoder" takes the image input, producing a short row of visual token tiles; prompt token tiles enter separately. Image and prompt tokens jointly enter a visibly drawn "Encoder–decoder" module, then exit toward region outputs. Use only these two named internal modules; show two lightly stacked computational layers inside the encoder–decoder, not a fake brain, chip mascot, dense network or magical light. Prompt tokens must not be routed through the image encoder. The technical abstraction is based on the Florence-2 paper's image encoder plus multimodal encoder-decoder, not an invented architecture. Right: a small clear panel labeled "Grounded regions" shows lens and sensor thumbnails with dashed candidate boxes matching the corresponding prompt colors. This is the visible transformation from semantic text to image positions. A short downstream arrow outside the model then goes to a modest footer module labeled "ROI refinement", with a simple circle-and-rectangle outline icon. Keep this stage secondary; Florence-2 does not directly output an exact fitted circle. A small "AOI" inset connected to the sensor candidate may indicate the subsequent cropped-region stage if it stays uncluttered; never present a one-shot exact inner sensor ROI as the model output. Do not retain the old generic Image + text / Florence-2 / Target regions footer in addition to the model. All labels must be large, legible at 410px wide; about 46px at 1536px source, short labels can wrap neatly. Use generous breathing room and very few arrows; no paragraphs, numerical results, unsupported model dimensions, logo, watermark, code, confidence scores or decorative dashboard cards. Conceptual illustration, not inference output.
