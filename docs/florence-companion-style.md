# Florence-2 companion-style correction

Historical wide-layout revision. The current asset rearranges the same content into two tiers; see [the latest layout record](florence-two-tier-layout.md).

The user requested natural object proportions, an elbow connector from the sensor candidate to the final AOI, less outer whitespace through cropping, and the restrained visual style of the adjacent Multi-focus illustration.

Final project asset: `assets/images/florence2-model-grounding.png` (2062 × 763), copied unmodified from `exec-eedfb55d-900d-42cd-a269-4b5927d917e4.png`. Built-in image_gen mode was used for the illustration edit; no CLI/API fallback. The reference `assets/images/multi-focus-trajectories.png` was used for style only and remains unchanged. A final user-requested refinement makes image/text token interaction visible inside the shared encoder–decoder and replaces the isolated output circle with an optical lens view bearing a fitted circle and center crosshair. The caption and accessible description were updated accordingly.

Intermediate style source: `exec-bd30f9ea-9bca-480f-81c8-c0a74f163e96.png` (1774 × 887). At the user's explicit request to crop, a literal System.Drawing bitmap crop retained rectangle x=0, y=112, width=1774, height=656. That crop became the input for the final token-interaction and lens-overlay edit. No resizing, stretching, warping or resampling was applied to either generated asset after generation. Natural dimensions in research.yml match the final saved PNG. The page presents a conceptual model schematic, not measured inference outputs; interaction arrows abstract multimodal fusion rather than asserting a particular attention implementation.

An intermediate natural-proportion and elbow-connector edit used `exec-096995d4-4d23-40d6-bab0-05b3c5041a1d.png`. The companion-style edit used that image as its target and the Multi-focus illustration as its style reference. Heavy enclosing cards were removed. The later token-interaction edit adds only two concise token labels. Semantic/candidate correspondence uses teal for the lens and amber for the sensor; the image encoder and encoder–decoder remain visible, followed by downstream ROI refinement. The outer-crop/inner-prompt workflow remains explained in the accompanying research text.

Validation: Jekyll build passed. At 1440/768/375/320px viewport widths, the displayed image aspect ratio matches its natural 2062:763 ratio; no horizontal overflow was found. Full-image links, all 15 research entries and the unchanged Multi-focus image were checked. Desktop and mobile screenshots were inspected.

## Proportion and connector prompt

Edit this existing Florence-2 illustration. Use the supplied ORIGINAL, naturally proportioned image as the edit target. The user rejects stretched objects/text and explicitly wants cropping instead of stretching to remove margins.

CHANGE ONLY:
1. Align the top of the rightmost "ROI refinement" panel with the top of the Florence-2 panel. Move the AOI chip up underneath the ROI panel so the bottom of that chip aligns with the bottom of the Florence-2 panel. Fit them by repositioning and, only if necessary, UNIFORM aspect-preserving scaling. The small output circle MUST be circular; the lens, square sensor packages, token blocks and all text MUST keep the original width-to-height proportions. Leave the original first three columns substantially unchanged.
2. Replace the diagonal blue dashed connector from the lower "sensor window" grounded candidate to the final AOI dashed boundary with a neat ORTHOGONAL ELBOW connector: a short horizontal run out of the candidate on its right, a vertical bend in the gutter, then a short horizontal segment to the AOI boundary on the left. It should visually connect the candidate to the final AOI, not run diagonally across the chip. Keep the separate vertical dashed connector from ROI refinement to AOI neat.
3. CROP OFF the blank top and bottom margins. Use a naturally WIDE landscape composition, around 2.3:1 if convenient, with a small even outer border. DO NOT force a 4:3 canvas. DO NOT stretch, warp or elongate any content to fill a canvas. If canvas dimensions require extra margin, leave it blank for a later literal crop instead of changing object proportions.

PRESERVE:
All existing content, exact spelling, pale sage glass-like style, gradients and shadows; the two optical image inputs; "Images", "Text prompts", "circular camera lens", "sensor window", "Florence-2", "Image encoder", "Image tokens", "Prompt tokens", "Encoder-decoder", "Grounded regions", "ROI refinement", "AOI". Keep Florence-2 visible with separate image and text pathways. Retain the lens candidate dashed teal box, sensor candidate dashed blue box, final yellow dashed inner AOI rectangle. No additional explanatory text, steps, decorations or labels. All photograph/render proportions and font letter shapes must remain as in the supplied original. This is a proportion-preserving alignment and crop correction, not a redesign.

## Final companion-style prompt

Use case: scientific-educational / style-transfer edit.
Image 1 is the Florence-2 diagram to EDIT. Image 2 is ONLY the VISUAL STYLE REFERENCE, the neighboring Multi-focus illustration on the same academic homepage.

Make Image 1 look like a companion to Image 2: refined realistic optical materials, quiet pale sage-gray background, translucent glass, subtle depth, fine dark-navy typography and restrained teal/amber accents. The user wants a beautiful, composed, simple scientific illustration, not a dense flowchart or a bright cartoon infographic.

Preserve the scientific story but simplify its presentation:
- Left: naturally proportioned realistic lens and sensor images, plus the two visible semantic prompts "circular camera lens" in teal and "sensor window" in amber. Group the image inputs with a small "Images" label, and the semantic phrases with "Text prompts". Remove the heavy rounded enclosing cards around these inputs. Retain the natural lens perspective, square sensor packages and undistorted typography.
- Center: ONE understated translucent model enclosure, headed "Florence-2", with a visible small "Image encoder" and a larger two-layer translucent "Encoder–decoder" model. Keep a very short row of visual-token tiles between the image encoder and encoder–decoder, without additional token labels. Clear image path: optical images -> image encoder -> tiles -> encoder–decoder. Clear text path: text prompts -> encoder–decoder, bypassing the image encoder. No disconnected token branch or bypass from the image encoder. A few elegant precise arrows, no maze.
- Right: "Grounded regions" with a lens candidate above and a sensor candidate below; matching teal and amber dashed candidate boundaries make semantic-to-image correspondence visible. Keep the two semantic colors consistent throughout. Do not add an extra large enclosing box around the output group.
- Far right, a compact secondary group labeled "ROI refinement": a teal geometric target circle at the same vertical center as the lens candidate, and beneath it the cropped sensor "AOI" with a dashed inner rectangle at the same vertical center as the sensor candidate. This is a downstream stage, outside Florence-2. Keep the lens route to the geometry circle distinct from the sensor route to AOI. The lower sensor candidate -> AOI connector must be an ORTHOGONAL ELBOW (horizontal, short vertical bend in the clear gutter, then horizontal), not a diagonal line across either image. It connects the outer candidate to the cropped AOI with its inner dashed region; do not imply the model directly predicts an exact circle or inner ROI. Right output objects should be aligned with their corresponding candidate objects. No tall last column dangling below the composition.

Simplification: remove redundant "Image tokens" and "Prompt tokens" labels, tiny repeated square decorations and the old thick blue/green panel outlines. Only the central model needs a subtle glass enclosure. Use the real optical objects and translucent model as the main visuals, not card borders. Fewer, larger elements. No new technical steps or paragraphs. Preserve the exact key words quoted above.

PROPORTIONS AND CROPPING ARE CRITICAL:
Keep all object and letter aspect ratios natural. A geometric circle is a true circle. Sensor packages are approximately square. NEVER elongate images or font glyphs to fill height. Aim for a natural wide composition around 1.8–2:1, not a forced 4:3. Closely frame the CONTENT with a small safe border; remove excessive top/bottom empty bands by cropping, not stretching or increasing vertical gaps. If the generated canvas has remaining blank margin, leave it plain for a literal later crop. No decorative rays, equations, performance metrics, token-count claims, extra titles or extra charts. This is a clearly conceptual scientific schematic, not an experimental output.

## Final token-interaction and optical-result prompt

Edit the supplied Florence-2 scientific homepage illustration with ONLY TWO targeted improvements. Preserve its refined pale sage background, realistic optical objects, clear glass model, navy text, restrained teal/amber accents, compact layout, aligned outputs and natural proportions.

1. MAKE IMAGE–TEXT TOKEN INTERACTION VISIBLE.
Keep the Image encoder. Inside the shared Encoder–decoder module, replace the two generic blue block stacks with a concise, elegant multimodal fusion depiction: an upper short row of 3–4 teal glass tiles labeled "Image tokens", and a lower short row of 3–4 amber glass tiles labeled "Text tokens". The encoded image path must enter the teal row; the existing prompt path must enter the amber row. Between the two rows show just 2–3 fine, visible connecting lines with small arrowheads, a restrained cross-modal interaction motif. Both rows are inside ONE shared Encoder–decoder model enclosure; they interact, rather than becoming two independent detectors. A light translucent shared layer behind them may unify them. Keep the model visibly three-dimensional and refined; do not fill it with dense networks, extra submodules or paragraphs. A shared output exits toward both grounded candidate regions. Token counts are illustrative. Avoid a separate path suggesting image tokens detect only lenses while text tokens detect only sensors. The model fuses both modalities before candidate grounding. Only add these two labels, "Image tokens" and "Text tokens"; do not add explanatory sentences.

2. REPLACE THE EMPTY OUTPUT CIRCLE WITH A REAL OPTICAL LOCALIZATION VISUAL.
At upper right under "ROI refinement", replace the plain empty teal circle with a small realistic, naturally proportioned lens/annular inspection view matching the adjacent lens candidate. Overlay ONE precise solid teal target circle on the optical structure and a tiny center crosshair or point. This makes the result a fitted ROI on an actual depicted lens, not an abstract isolated shape. The coarse candidate remains the dashed teal box on its left, with an arrow to this refined solid circle on the optical image. Refinement is outside Florence-2. Keep the lower AOI sensor image and its inner dashed rectangle, the orthogonal elbow connector from the sensor candidate, and their alignment unchanged.

All other text and content stays as supplied. Preserve natural width/height of lenses, chips, circles and font glyphs. No stretching, warping, tall empty cards, heavy outlines, extra labels, numeric accuracy claims or busy decorative arrows. Maintain the compact wide composition and small outer margins; crop spare background instead of elongating content. This remains a conceptual illustration, not a claim of real measured output.
